#include <bits/stdc++.h>
using namespace std;
int main()
{

    string s;
    cin >> s;
    cout << s[0] << endl;
    cout << s.at(3) << endl; // Low use in code.
    cout << s.back() << endl; // Most use
    cout << s.front() << endl; // last index s[0]
    return 0;
}